<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?>
<!DOCTYPE Function SYSTEM "../DTD/Function.dtd">
<Function>
	<KnowledgeBase>
        <Title>isub</Title>
		<FunctionName>isub</FunctionName>
		<FunctionType>Builtin(Math)</FunctionType>
		<ArgumentTypes>Number Integer</ArgumentTypes>
		<DataType>Integer</DataType>
		<RelatedFunctions>  addi divi  muli subi modi iadd idiv imul  imod</RelatedFunctions>
        <RelatedVMInstructions> vmiadd vmidiv vmidivr vmimul   </RelatedVMInstructions>
		<HumanKeywords> number arithmetic subtraction integer operators </HumanKeywords>
	</KnowledgeBase>
         <Description><![CDATA[
             <p> The <b>isub</b> is a built-in function that returns the difference all of its 
              arguments starting from the leftmost arguments and returns the result as an 
              Integer quantity.   </p>
          ]]></Description>
	<Brief>
	   <Overview><![CDATA[
		<p>The <b>isub</b> is a built-in function that returns the difference all of its 
          arguments starting from the leftmost arguments and returns the result as an Integer 
          quantity.  The operands are not checked and are not coerced.  
        </p>
        ]]></Overview>
        <Usage><![CDATA[
        <p>Use the <b>isub </b> to compute the difference of an arbitrary number of Integer 
          operands. The <b>isub</b> built-in function is a simple but very fast integer 
          subtraction function.  Use the <b>isub</b> function for highly optimized integer 
          subtraction.  An alternate function, <b>subi</b>, will take any numeric operand and
          will perform type conversion before subtracting and returns an integer result, 
          but is much slower.
        </p>
         ]]></Usage>
    </Brief>
	<Syntax>
	    <Expression><![CDATA[<p><b>  (isub  int1 int2, ... ) </b></p>
]]></Expression>
		<Argument>
		  <Name>int1</Name>
		  <Note><![CDATA[
		  <p>The first operand.</p>
          ]]></Note>
		  <AISTypes>Integer</AISTypes>
		</Argument>
		<Argument>
		  <Name>int2</Name>
		  <Note><![CDATA[
		  <p>An arbitrary number of subsequent operands.</p>
          ]]></Note>
		  <AISTypes>Integer</AISTypes>
		</Argument>
		<Return><![CDATA[
	    <p>The difference all of its arguments starting from the leftmost arguments 
           expressed as an Integer quantity
	    </p>
	    ]]></Return>
	</Syntax>
</Function>
